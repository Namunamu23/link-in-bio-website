# /portfolio

Drop the real portfolio PDF here.

The homepage button links to `portfolio/Portfolio_Ana.pdf` by default — update the filename in `data/content.js` under `portfolio.href` if you use a different name.
